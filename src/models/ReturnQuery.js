export default class ReturnQuery {
    constructor(radix, return_params) {
        this.radix = radix;                 // Instance of ChartData
        this.return_params = return_params; // Instance of ReturnParams
    }
}

